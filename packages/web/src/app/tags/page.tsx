import { PlaceholderView } from '@/components/common/placeholder-view'

export default function TagsPage() {
  return (
    <PlaceholderView
      title="标签"
      desc="标签页暂未实现，后续将支持按主题聚合文章。"
    />
  )
}
